services:
  ffc-sfd-landing-page:
    build:
      context: .
      target: production
    image: ffc-sfd-landing-page
    container_name: ffc-sfd-landing-page
    environment:
      APPINSIGHTS_CLOUDROLE: ffc-sfd-landing-page-local
      APPINSIGHTS_CONNECTIONSTRING: ${APPINSIGHTS_CONNECTIONSTRING}
      AUTH_HOST: ${AUTH_HOST:-http://ffc-sfd-proxy:3000}
      GATEWAY_HOST: ${GATEWAY_HOST:-http://localhost:3000}
      DATA_HOST: ${DATA_HOST:-http://ffc-sfd-data:3004/graphql}
